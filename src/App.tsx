import AudioUploadCard from "./AudioUploadCard.js";

export default function App() {
  return (
    <main className="grid min-h-screen place-items-center p-6">
      <AudioUploadCard />
    </main>
  );
}
